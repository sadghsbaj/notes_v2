# External Library Documentation

Reference documentation for AI agents. Search here when you need API details or usage patterns.

## Libraries

| Library | Path | Purpose |
|---------|------|---------|
| **SolidJS** | `solidjs/` | Core framework + Router |
| **Vanilla Extract** | `vanilla-extract/` | CSS-in-TS, theming, APIs |
| **Capacitor** | `capacitor/` | Native Android (CLI, Plugins) |
| **Tiptap** | `tiptap/` | Rich text editor |

## Usage

When uncertain about an API, search the relevant folder:

```
# Example: How does createTheme work?
→ Look in vanilla-extract/13_documentation_api_createtheme.md

# Example: Capacitor Filesystem plugin
→ Look in capacitor/plugins/
```

## Structure

- Files are numbered for logical reading order
- Nested folders for complex topics (CLI, plugins, router)
- All content is markdown for easy searching
